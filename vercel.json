{
    "version": 2,
    "builds": [
      {
        "src": "./app_token.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/api/app_token.py" }
    ]
}